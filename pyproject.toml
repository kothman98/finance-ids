[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "finance-ids"
version = "1.0.0"
description = "Deterministic transaction ID generation for finance pipelines"
requires-python = ">=3.11"
